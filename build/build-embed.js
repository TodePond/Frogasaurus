const greet = (name) => console.log(`Hello ${name}!`)

// Auto-generated by some MotherTode
(() => {

			const global = window
			const scope = {}
			const term = Term.string(`Hello world!`)
			for (const key in term) {
				scope[key] = term[key]
			}
			return term
		
})()